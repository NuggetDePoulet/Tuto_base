////////TERMINAL//////////
Voici un "plugin" a rentrer dans votre terminal pour vous rendre la vie facile.
site sur un moteur de recherche = Oh my zsh


sh -c "$(wget https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh -O -)"

pwh = Vous permet de vous localiser
norminette = verifier sur tu es a la norme (tres important avant de rendre)
cd = aller dans un ficher
cd .. = ficher percedent 
ls = lister les ficher non cacher
ls -a = lister les fichiers en details et les dossier cacher
mkdir = creer un dossier
touch = creer un fichier
rm -rf = supprimer le fichier (!!A UTILISER AVEC PARCIMONIE!!)
rm = remove soit supprimer un fichier

//////////VIM/////////

vimtutor = le tutoriel de vim
vim plus du nom du fichier = A ouvrir un fichier
: = Formule pour lancer une commande (ex :x pour enregister)
:x = enregistrer et quitter
La touche I = --INSERT--
La touche ESC = annule les changement de mode (ex --INSERT--)
:w = sert a enregister
:q = sert a quitter

///////git////////

git clone "repository"/"nom du fichier" = creer un dosier git 
git add = premier ajout d'un ficher a git
git commit -m "message" = d'ajout d'un message pour le git add "obligatoir"
git push = ajoute le fichier au server  
git status = sert a verifier si vous avez des add et ou des commit 



///////le C//////

void    ft_putchar(char c) = 
{
        write(1, &c, 1);
}
